# GitHub Pages 部署指南

## 自动部署到网页版

现在您的应用会自动构建并部署为网页版本，任何人都可以通过浏览器访问！

---

## 📋 部署步骤

### 步骤 1: 推送代码到 GitHub

```bash
cd /Users/henry/Documents/2026/CodeBuddy/EnReading

# 添加新的工作流文件
git add .github/workflows/deploy-web.yml
git add GITHUB_PAGES_GUIDE.md

# 提交
git commit -m "Add GitHub Pages deployment"

# 推送到 GitHub
git push origin main
```

### 步骤 2: 在 GitHub 启用 Pages

1. 打开您的 GitHub 仓库页面
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages**
4. 在 **Source** 下选择:
   - Source: **GitHub Actions** (不是 Deploy from a branch)
5. 点击 **Save** (保存)

### 步骤 3: 等待构建完成

1. 返回仓库首页，点击 **Actions** 标签
2. 会看到 "Deploy to GitHub Pages" 工作流正在运行
3. 等待 5-8 分钟，直到显示绿色 ✅
4. 构建成功后，您的应用会自动部署

### 步骤 4: 访问您的网页应用

访问地址:
```
https://你的GitHub用户名.github.io/EnReading/
```

例如，如果您的用户名是 `henry123`，访问:
```
https://henry123.github.io/EnReading/
```

---

## 🎯 重要提示

### ⚠️ 首次部署注意事项

1. **必须在 Settings → Pages 中选择 "GitHub Actions" 作为 Source**
   - 如果选择 "Deploy from a branch"，部署会失败
   - 正确的选项是 **GitHub Actions**

2. **仓库必须是 Public (公开)**
   - 如果是 Private (私有) 仓库，需要 GitHub Pro 账户
   - 检查方法: Settings → General → Danger Zone → Change repository visibility

3. **base-href 路径配置**
   - 已自动配置为 `/EnReading/`
   - 必须与您的仓库名称匹配
   - 如果仓库改名，需要更新 `.github/workflows/deploy-web.yml` 中的 `--base-href`

---

## 📊 查看部署状态

### 方法 1: Actions 页面
1. 进入仓库，点击 **Actions** 标签
2. 查看最新的 "Deploy to GitHub Pages" 工作流
3. 绿色 ✅ = 成功，红色 ❌ = 失败

### 方法 2: Deployments
1. 在仓库右侧栏找到 **Deployments**
2. 点击 **github-pages**
3. 可以看到所有部署历史和访问链接

---

## 🔄 更新应用

每次推送代码到 `main` 分支，都会自动:
1. 重新构建 Web 版本
2. 部署到 GitHub Pages
3. 更新在线应用

```bash
# 修改代码后
git add .
git commit -m "Update features"
git push origin main

# 等待 5-8 分钟，网页自动更新
```

---

## 🌐 分享您的应用

部署成功后，您可以将网址分享给任何人:
- 无需安装任何软件
- 直接在浏览器打开
- 支持电脑和手机浏览器
- 完全免费托管

---

## ❓ 常见问题

**Q: 推送代码后没有触发构建?**
A: 检查:
- 文件路径是否正确: `.github/workflows/deploy-web.yml`
- 是否推送到 `main` 分支 (不是 `master`)
- 查看 Actions 标签是否有错误提示

**Q: 构建成功但访问 404?**
A: 检查:
- Settings → Pages → Source 是否选择 "GitHub Actions"
- 仓库是否为 Public
- 访问的 URL 是否正确 (用户名和仓库名)
- 等待 1-2 分钟，GitHub Pages 可能需要时间同步

**Q: 页面空白或资源加载失败?**
A: 检查:
- `--base-href "/EnReading/"` 是否与仓库名称匹配
- 浏览器控制台是否有 404 错误
- 如果仓库名不是 "EnReading"，需要修改 base-href

**Q: 可以自定义域名吗?**
A: 可以! 
1. 在 Settings → Pages → Custom domain 添加域名
2. 在域名 DNS 设置中添加 CNAME 记录指向 `你的用户名.github.io`
3. 更新 `--base-href "/"` (移除仓库名)

**Q: 构建时间太长?**
A: 
- 正常构建时间: 5-8 分钟
- 可以在 Actions 页面查看详细进度
- GitHub Actions 免费额度: 2000 分钟/月

**Q: 如何查看在线应用的访问链接?**
A: 三种方法:
1. Settings → Pages 页面顶部会显示
2. Actions → 最新工作流 → deploy 任务中的 URL
3. 仓库右侧 Deployments → github-pages

---

## 🛠️ 手动触发构建

如果想在不推送代码的情况下重新部署:

1. 进入 Actions 标签
2. 点击左侧 "Deploy to GitHub Pages"
3. 点击右侧 "Run workflow" 按钮
4. 选择 `main` 分支，点击 "Run workflow"

---

## 📱 同时保留 Android APK 构建

不用担心! 现在您有两个工作流:
- `build.yml` - 构建 Android APK (保持不变)
- `deploy-web.yml` - 构建并部署 Web 版本 (新增)

每次推送代码，两个都会自动运行:
- APK 文件在 Actions → Build Android APK → Artifacts
- Web 版本自动部署到 GitHub Pages

---

## 🎉 总结

执行以下命令完成部署:

```bash
cd /Users/henry/Documents/2026/CodeBuddy/EnReading
git add .
git commit -m "Add web deployment"
git push origin main
```

然后:
1. 打开 GitHub 仓库 → Settings → Pages
2. Source 选择 "GitHub Actions"
3. 等待 5-8 分钟
4. 访问 `https://你的用户名.github.io/EnReading/`

完成! 🚀
