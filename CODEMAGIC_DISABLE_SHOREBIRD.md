# 如何在 Codemagic 中跳过 Shorebird

## 📌 问题说明

当你看到 "Publish updates to user devices using Shorebird" 时，这是 Codemagic 询问你是否要使用 Shorebird 热更新功能。

**答案：不需要！** 我们只是要构建一个普通的 APK 文件。

---

## ✅ 解决方法

### 方法 1：点击 "Skip" 或 "No" （最简单）

在 Codemagic 界面中：

```
┌─────────────────────────────────────┐
│ Publish updates to user devices     │
│ using Shorebird                      │
│                                      │
│ [Yes, enable Shorebird]              │
│ [Skip] or [No] ← 点这个！            │
└─────────────────────────────────────┘
```

**操作**：点击 **"Skip"**、**"No"** 或 **"Not now"** 按钮

---

### 方法 2：关闭 Shorebird 开关

如果看到开关选项：

```
┌─────────────────────────────────────┐
│ Integrations                         │
│                                      │
│ Shorebird    [OFF] ← 确保是关闭状态  │
│ Firebase     [OFF]                   │
│ Google Play  [OFF]                   │
│                                      │
│ [Continue]                           │
└─────────────────────────────────────┘
```

**操作**：确保 Shorebird 是 **OFF** 状态，然后点击 "Continue"

---

### 方法 3：在配置中取消勾选

如果看到复选框：

```
┌─────────────────────────────────────┐
│ Build Configuration                  │
│                                      │
│ ☐ Enable Shorebird ← 不要勾选！      │
│ ☐ Firebase integration               │
│ ☐ Google Play publishing             │
│                                      │
│ [Save & Build]                       │
└─────────────────────────────────────┘
```

**操作**：确保 Shorebird 前面的框**没有勾选**，然后继续

---

## 🎯 关键点

### Shorebird 是什么？

Shorebird 是一个 Flutter 热更新服务，允许你无需重新发布 APK 就能更新应用。

### 我们需要它吗？

**不需要！** 原因：
- ❌ 本项目是个人使用，不需要热更新
- ❌ 热更新需要付费订阅
- ❌ 增加构建复杂度
- ✅ 我们只需要一个简单的 APK 文件

---

## 📸 可能出现的界面

### 界面 1：询问是否启用

```
Do you want to enable Shorebird?

Shorebird allows you to push updates to your 
users without rebuilding your app.

[Enable Shorebird]  [Skip] ← 点 Skip
```

### 界面 2：需要 Token

```
Shorebird token is required

[Enter token]  [Skip this step] ← 点 Skip this step
```

### 界面 3：集成列表

```
Select integrations (optional):
☐ Shorebird    ← 不要勾选
☐ Firebase
☐ App Store Connect

[Continue without integrations] ← 点这个
```

---

## 🚀 继续构建

跳过 Shorebird 后，继续正常的构建流程：

1. ✅ 确认 Shorebird 已跳过/禁用
2. ✅ 确认构建配置：
   - Project type: Flutter App
   - Platform: Android
   - Build mode: Release
3. ✅ 点击 "Start new build"
4. ✅ 等待构建完成
5. ✅ 下载 APK

---

## 💡 如果找不到跳过按钮

### 尝试这些操作：

1. **向下滚动页面** - 按钮可能在下方
2. **查找 "Continue" 或 "Next"** - 点击继续下一步
3. **查找 "X" 或关闭图标** - 关闭 Shorebird 提示
4. **刷新页面** - 重新加载配置

---

## 🔄 如果还是不行

### 最终解决方案：使用 GitHub Actions

如果 Codemagic 强制要求 Shorebird 且无法跳过，**立即切换到 GitHub Actions**：

```bash
# 1. 上传到 GitHub
cd /Users/henry/Documents/2026/CodeBuddy/EnReading
git init
git add .
git commit -m "Initial commit"

# 2. 创建 GitHub 仓库（访问 github.com/new）

# 3. 推送代码
git remote add origin https://github.com/你的用户名/EnReading.git
git branch -M main
git push -u origin main

# 4. 访问仓库的 "Actions" 标签，自动开始构建
```

**GitHub Actions 优势**：
- ✅ 不需要任何第三方集成
- ✅ 配置已完整
- ✅ 完全免费
- ✅ 更简单直接

---

## 📋 总结

### 看到 "Publish updates using Shorebird" 时：

1. **立即点击**：Skip / No / Not now / Continue without
2. **不要点击**：Yes / Enable / Sign up
3. **不要输入**：任何 Token 或密钥

### 我们的目标：

只构建一个普通的 APK 文件，不需要任何额外的集成服务。

---

## ⚠️ 重要提醒

### 这些集成都是可选的，全部可以跳过：

- ❌ Shorebird - 热更新（不需要）
- ❌ Firebase - 后端服务（不需要）
- ❌ Google Play - 应用商店发布（不需要）
- ❌ App Store Connect - iOS 发布（不需要）
- ❌ TestFlight - iOS 测试（不需要）

### 我们只需要：

- ✅ Flutter SDK
- ✅ Android 构建工具
- ✅ 生成 APK 文件

**就这么简单！**

---

## 🎯 快速操作指南

当前在 Codemagic 界面：

1. **看到 Shorebird 提示** → 点击 "Skip" 或 "No"
2. **看到集成列表** → 不勾选任何项，点击 "Continue"
3. **看到询问 Token** → 点击 "Skip this step"
4. **看到构建配置** → 直接点击 "Start build"

**目标**：尽快到达 "Start new build" 按钮并点击它！

---

## 📞 需要帮助？

如果你：
- 找不到跳过按钮
- 被强制要求输入 Token
- 无法继续下一步

**截图发给我**，或**直接切换到 GitHub Actions**（推荐）

GitHub Actions 不会有这些问题，配置完整且稳定！🚀
